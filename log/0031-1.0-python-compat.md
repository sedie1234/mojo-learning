# work 0031 — [1.0] Python 호환성 회귀

- **status**: done
- **date**: 2026-05-11
- **cycle**: [1.0] cycle-3
- **plane**: MOJO-41

## 1) 무엇을 하고자 하는지

0.26 work 0006의 Python interop lifecycle/변환/예외 측정을 1.0에서 재실행해 *비용 모델 변화 여부* 확인.

## 2) 수행한 일

`01_lifecycle.mojo` — math/numpy cold·warm, sys first, type 변환 4종, 예외 통합 검증.

## 3) 예상되는 결과

cycle-1 work 0020 결과 — `from memory` → `from std.memory` 권장 외 *interop 표면 변화 미발견*. 비용 모델 동일 가능성 큼.

## 4) 실제 결과

| 측정 | 0.26 (work 0006) | **1.0** | 변화 |
|------|--:|--:|--:|
| math cold (Py_Initialize 포함) | 17 ms | **25.7 ms** | 측정 변동 (cold 디스크 캐시 상태 영향) |
| math warm (median) | 310~640 ns | **360 ns** | 동일 범위 |
| sys first import | 410 ns | **500 ns** | 동일 범위 |
| numpy cold | 39~64 ms | **40.9 ms** | 동일 범위 |
| numpy warm | 1.79 μs | **2.12 μs** | 동일 범위 |

Type 변환 4종 (`Int(py=po)`, `Float64(py=po)`, `String(py=po)`, `PythonObject` 그대로 사용): **모두 동일 syntax + 정상 동작**.

예외 통합 (`try/except e:`): **0.26과 동일 동작**. `division by zero` 잡힘.

## 5) 결론

- **Python interop 비용 모델 = 0.26 ↔ 1.0 *동일***. 절대 수치는 머신 상태 변동 범위.
- **Type 변환 API 변화 없음**: `Int(py=po)` keyword-only 그대로.
- **예외 통합 변화 없음**: `try/except e:` 동일.
- 단 *import 경로*는 1.0에서 `from std.python` 권장 (0.26 후반부터 동일).

→ Python interop은 **cycle-1 결론 그대로 유지**. 마이그레이션 비용 0.

후속:
- GIL 거동 + `parallelize` 결합 (0.26 work 0006 후속 후보, 본 cycle도 미정복)
- Python except 타입별 discriminate (cycle-1 미정복)
